{
  "name": "shikari-game",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "commit": "cz",
    "dev": "cd client && pnpm dev",
    "postinstall": "cd client && pnpm install",
    "format:win": "prettier --config ./.prettierrc.js --loglevel silent --write \"**/*.{js,tsx,css}\"\"",
    "format:mac": "prettier --config ./.prettierrc.js --write --loglevel silent '**/*.{js,tsx,css}'"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "commitizen": "^4.2.5",
    "prettier": "^2.7.1"
  }
}
